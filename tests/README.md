# Tests

This folder contains executable test scripts and validation checks.

## Current files

- `tests/test_fno_surrogate.py`
- `tests/test_pino_surrogate.py`

If these evolve into formal unit tests, consider adding a test runner config
(for example `pytest.ini`) and grouping by feature area.
