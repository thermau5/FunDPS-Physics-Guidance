# Legacy Model Artifacts

This folder is for legacy surrogate checkpoints that were previously stored in
code directories (for example `generation/*.pth`).

- Put historical `.pth` / `.pkl` files here.
- Keep this folder ignored by default via `.gitignore`.
- If a model must be versioned in git, place it under
  `artifacts/models/curated/` instead.
